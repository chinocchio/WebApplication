@model WebApplication2.ViewModels.PropertyListViewModel

@{
    ViewData["Title"] = "Sales Transactions";
    Layout = "_Layout";
}

<h2>Sales Transactions</h2>

<form method="get">
    <input type="text" name="searchTerm" value="@Model.SearchTerm" placeholder="Search Contract No, Fullname, or Unit Code" />
    <button type="submit">Search</button>
</form>

@if (!string.IsNullOrWhiteSpace(Model.SearchTerm))
{
    if (Model.SalesTransactions != null && Model.SalesTransactions.Any())
    {
        <ul class="nav nav-tabs mt-4" id="transactionTabs" role="tablist">
            <li class="nav-item" role="presentation">
                <a class="nav-link active" id="sales-tab" data-bs-toggle="tab" href="#sales" role="tab">Sales Info</a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link" id="buyer-tab" data-bs-toggle="tab" href="#buyer" role="tab">Buyer Info</a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link" id="property-tab" data-bs-toggle="tab" href="#property" role="tab">Property Info</a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link" id="proponent-tab" data-bs-toggle="tab" href="#proponent" role="tab">Sales Proponent Info</a>
            </li>
        </ul>

        <div class="tab-content border p-3" id="transactionTabsContent">
            @section Scripts {
                <script>
                    document.addEventListener("DOMContentLoaded", function () {
                        document.querySelectorAll(".clickable-row").forEach(row => {
                            row.addEventListener("click", function () {
                                const targetId = this.getAttribute("data-bs-target");
                                const contentRow = document.getElementById(targetId);
                                if (contentRow) {
                                    contentRow.classList.toggle("d-none");
                                }
                            });
                        });
                    });
                </script>
    }

            <div class="tab-pane fade show active" id="sales" role="tabpanel">
                <table class="table mt-3">
                    <thead>
                        <tr>
                            <th>Contract Number</th>
                            <th>Type of Sale</th>
                            <th>Holding Date</th>
                            <th>Promo Discount</th>
                            <th>Transaction Type</th>
                            <th>Milestone</th>
                            <th>Status Color</th>
                            <th>Status In General</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var transaction in Model.SalesTransactions)
                        {
                            var targetId = $"details_{transaction.SalesTransactionId}";

                            <tr class="clickable-row table-primary" style="cursor:pointer;" data-bs-target="@targetId">
                                <td>@transaction.ContractNumber</td>
                                <td>@transaction.TypeOfSale</td>
                                <td>@transaction.HoldingDate</td>
                                <td>@transaction.PromoDiscount</td>
                                <td>@transaction.TransactionType</td>
                                <td>@transaction.Milestone</td>
                                <td>@transaction.NewColorStatus</td>
                                <td>@transaction.StatusInGeneral</td>
                            </tr>

                            <tr id="@targetId" class="d-none bg-light">
                                <td colspan="8">
                                    @if (transaction.ReservationFee != null)
                                    {
                                        <dl class="row p-3">
                                            <dt class="col-sm-3">Rf Date Paid:</dt>
                                            <dd class="col-sm-9">@transaction.ReservationFee.RfDatePaid</dd>

                                            <dt class="col-sm-3">Proof Of RF Used Upon Holding:</dt>
                                            <dd class="col-sm-9">@transaction.ReservationFee.RfProofUsedUponHolding</dd>

                                            <dt class="col-sm-3">RF Amount Paid To Unit:</dt>
                                            <dd class="col-sm-9">@transaction.ReservationFee.RfAmountPaidToUnit</dd>

                                            <dt class="col-sm-3">RF Amount Paid To GMTOE:</dt>
                                            <dd class="col-sm-9">@transaction.ReservationFee.RfAmountPaidToGMTOE</dd>

                                            <dt class="col-sm-3">RF Amount (UNIT+GMTOE) Total:</dt>
                                            <dd class="col-sm-9">@transaction.ReservationFee.RfAmountGMTOE_Unit</dd>

                                            <dt class="col-sm-3">Date RF Credited:</dt>
                                            <dd class="col-sm-9">@transaction.ReservationFee.RfDateCredited</dd>

                                            <dt class="col-sm-3">RF OR Number:</dt>
                                            <dd class="col-sm-9">@transaction.ReservationFee.RfOrNumber</dd>
                                        </dl>
                                    }
                                    else
                                    {
                                        <em class="text-muted">No reservation fee data available.</em>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <div class="tab-pane fade" id="buyer" role="tabpanel">
                <table class="table mt-3">
                    <thead>
                        <tr>
                            <th>Client's Name</th>
                            <th>Role</th>
                            <th>Customer Code</th>
                            <th>Client Base</th>
                            <th>Contact Number</th>
                            <th>Email Address</th>
                            <th>ID Sbmitted</th>
                            <th>Date ID Submitted</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var transaction in Model.SalesTransactions)
                        {
                            if (transaction.BusinessPartner?.OtherBuyers != null)
                            {
                                foreach (var buyer in transaction.BusinessPartner.OtherBuyers)
                                {
                                    <tr>
                                        <td>@buyer.Fullname</td>
                                        <td>@buyer.Role</td>
                                        <td>@buyer.CustomerCode</td>
                                        <td>@buyer.ClientBase</td>
                                        <td>@buyer.ContactNumber</td>
                                        <td>@buyer.EmailAddress</td>
                                        <td>@buyer.IdSubmitted</td>
                                        <td>@buyer.IdDateSubmitted</td>
                                    </tr>
                                }
                            }
                        }
                    </tbody>
                </table>
                <!-- AFTER buyers table, Credit Review tab system -->

                <ul class="nav nav-tabs" id="creditReviewTabs" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active" id="credit-tab" data-bs-toggle="tab" data-bs-target="#credit" type="button" role="tab" aria-controls="credit" aria-selected="true">
                            Credit Review
                        </button>
                    </li>
                </ul>

                <div class="tab-content border p-3" id="creditReviewTabsContent">
                    <div class="tab-pane fade show active" id="credit" role="tabpanel" aria-labelledby="credit-tab">
                        @foreach (var transaction in Model.SalesTransactions)
                        {
                            if (transaction.CreditReview != null)
                            {
                                <div class="mb-4">
                                    <dl class="row">
                                        <dt class="col-sm-3">Credit Review Result:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.CreditReviewResult</dd>

                                        <dt class="col-sm-3">CMAP Result:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.CMAPResult</dd>

                                        <dt class="col-sm-3">Credit Review Remarks:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.CreditReviewRemarks</dd>

                                        <dt class="col-sm-3">NDI Status:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.NDIStatus</dd>

                                        <dt class="col-sm-3">Is Bank Approvable:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.IsBankApporvable</dd>

                                        <dt class="col-sm-3">Red Tag:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.RedTag</dd>

                                        <dt class="col-sm-3">Red Tag Reason:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.RedTagReason</dd>

                                        <dt class="col-sm-3">CI Completion Date:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.CiCompletionDate.ToString("MM/dd/yyyy")</dd>

                                        <dt class="col-sm-3">Type of Income:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.TypeOfIncome</dd>

                                        <dt class="col-sm-3">NDI Rate:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.NdiRate</dd>

                                        <dt class="col-sm-3">Net Disposable Income:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.NetDisposableIncome</dd>

                                        <dt class="col-sm-3">Other Loans:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.OtherLoans</dd>

                                        <dt class="col-sm-3">Net NDI:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.NetNdi</dd>

                                        <dt class="col-sm-3">NDI vs Bank MA To Be Amount:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.NdiVsBankMaTobAmt</dd>

                                        <dt class="col-sm-3">Percent of NDI vs MA:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.PercentOfNdiVsMa</dd>

                                        <dt class="col-sm-3">NDI Category:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.NdiCategory</dd>

                                        <dt class="col-sm-3">Max Term:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.MaxTerm</dd>

                                        <dt class="col-sm-3">Estimated Max Term:</dt>
                                        <dd class="col-sm-9">@transaction.CreditReview.EstimatedMaxTerm</dd>
                                    </dl>
                                </div>
                            }
                            else
                            {
                                <p class="text-muted">No Credit Review information available for this transaction.</p>
                            }
                        }
                    </div>
                </div>
            </div>

            <div class="tab-pane fade" id="property" role="tabpanel">
                <table class="table mt-3">
                    <thead>
                        <tr>
                            <th>Property Type</th>
                            <th>Project Name</th>
                            <th>Building/Phase</th>
                            <th>Unit Code</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var transaction in Model.SalesTransactions)
                        {
                            <tr>
                                <td>@transaction.Properties?.PropertyType</td>
                                <td>@transaction.Properties?.ProjectName</td>
                                <td>@transaction.Properties?.BuildingPhase</td>
                                <td>@transaction.Properties?.UnitCode</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <div class="tab-pane fade" id="proponent" role="tabpanel">
                <table class="table mt-3">
                    <thead>
                        <tr>
                            <th>Broker</th>
                            <th>PS/QC/ISM</th>
                            <th>Marketing Officer</th>
                            <th>Markerting Manager</th>
                            <th>Deputy Markerting Director</th>
                            <th>Marketing Director</th>
                            <!-- Add more fields if needed -->
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var transaction in Model.SalesTransactions)
                        {
                            <tr>
                                <td>@transaction.SalesProponent?.Broker</td>
                                <td>@transaction.SalesProponent?.PS_QC_ISM</td>
                                <td>@transaction.SalesProponent?.MarketingOfficer</td>
                                <td>@transaction.SalesProponent?.MarketingManager</td>
                                <td>@transaction.SalesProponent?.DeputyMarketingDirector</td>
                                <td>@transaction.SalesProponent?.MarketingDirector</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
    else
    {
        <p class="text-danger mt-4">No results found.</p>
    }
}
